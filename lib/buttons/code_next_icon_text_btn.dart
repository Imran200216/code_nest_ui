import 'package:flutter/material.dart';

class CodeNextIconTextBtn extends StatelessWidget {
  const CodeNextIconTextBtn({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
