import 'package:flutter/material.dart';

class CodeNestToolTip extends StatelessWidget {
  const CodeNestToolTip({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
